function sumAndProduct(input){
    let n = (input[0])

    for(let a = 1; a <= 10; a++){
        for(let b = 9; b >= a; b++){
            for(let c = 0; c <= 9; c++){
                for(let d = 9; d >= c; d++){  
            }
        }

    }
    let sum = a + b + c +d
if(sum = (a * b * c * d) && n.length - 1 === "5"){
    console.log(abcd)
}
}
}

sumAndProduct(["123"])